# إصلاح التسليم + قائمة ما قبل الإطلاق

## المشكلة

```
فشل التسليم: sign failed for f06-letters-numbers.pdf: 400
{"statusCode":"404","error":"not_found","message":"Object not found","code":"NoSuchKey"}
```

**السبب:** ملفات المنتجات لم تُرفع إلى مخزن Supabase أبداً. الكود يطلب من Supabase
توقيع رابط تحميل مؤقت لملف اسمه `f06-letters-numbers.pdf` داخل bucket اسمه `files`،
وSupabase يرد: لا يوجد ملف بهذا الاسم (`NoSuchKey`).

الملفات موجودة على جهازك في:

```
C:\Users\howic\OneDrive\Desktop\sis\khamsa-files-upload\
```

وأسماؤها الستة عشر مطابقة تماماً لما ينتظره الكود — تحتاج فقط أن تُرفع.

---

## الحل — اختر طريقة واحدة

### الطريقة أ: سكربت جاهز (دقيقة واحدة)

من Supabase → **Settings → API** انسخ `Project URL` و`service_role key`، ثم:

```bat
cd C:\Users\howic\OneDrive\Desktop\sis\khamsa-site
set SUPABASE_URL=https://xxxx.supabase.co
set SUPABASE_SERVICE_KEY=eyJ...
python tools/upload_files.py
```

السكربت ينشئ الـ bucket إن لم يكن موجوداً، يرفع الناقص فقط، ثم يتحقق ويطبع النتيجة.
لفحص الوضع دون رفع: `python tools/upload_files.py --check`

> يعمل بمكتبات بايثون القياسية — لا يحتاج تثبيت أي شيء، ولا يحتاج Node.

### الطريقة ب: يدوياً من لوحة Supabase

1. Supabase → **Storage** → إن لم يوجد bucket باسم `files` أنشئه، و**اتركه Private**.
2. ادخل الـ bucket → **Upload files** → اسحب الملفات الستة عشر من مجلد
   `khamsa-files-upload` دفعة واحدة.
3. تأكد أن الأسماء لم تتغيّر (بدون مسافات أو `(1)` في النهاية).

> **مهم:** أبقِ الـ bucket **Private**. لو جعلته Public يصبح أي شخص يخمّن الرابط
> قادراً على تحميل منتجاتك مجاناً، وتفقد فائدة الروابط المؤقتة.

---

## بعد الرفع

1. شغّل SQL المحدّث مرة واحدة: Supabase → SQL Editor → الصق محتوى
   `supabase-setup.sql` → Run. (يضيف عمود `delivered` — مشروح تحت.)
2. ارفع تعديلات الكود إلى GitHub ليعيد Vercel النشر.
3. من لوحة الإدارة اضغط **🔍 فحص جاهزية الملفات** — لازم تطلع
   «✅ كل الملفات جاهزة (16/16)».
4. جرّب **🚚 تسليم الطلب الآن** على إيميلك.

---

## هل يصل الملف للعميل مباشرة بعد الدفع؟

**نعم — هذا هو التصميم:**

1. العميل يدفع (Paylink) → يُحوّل إلى `/thanks`
2. الصفحة تنادي `/api/verify-pay` الذي يتحقق من الدفعة **من سيرفر البوابة مباشرة**
   ويقارن المبلغ بسعر المنتج
3. يُسجَّل الطلب (لمنع التكرار) ثم تُوقَّع روابط التحميل
4. العميل يرى أزرار التحميل **فوراً في نفس الصفحة**، ويصله **إيميل** فيه نفس
   الروابط (صالحة 7 أيام)

**لكن قبل هذا الإصلاح كان الواقع مختلفاً:** بما أن الملفات غير مرفوعة، كان
التوقيع يفشل، فترمي الدالة استثناءً، وتُرجع 500 — وصفحة الشكر كانت تعرض
**«ما اكتمل الدفع»** رغم أن الدفع تم فعلاً والمبلغ خُصم.

هذا أسوأ سيناريو ممكن: العميل يدفع، لا يستلم شيئاً، ويُقال له إن دفعته فشلت.

---

## ما أُصلح في الكود

| الملف | التعديل | لماذا |
|---|---|---|
| `api/_delivery.js` | رسالة خطأ تشرح السبب والحل بدل رسالة Supabase الخام | كانت `NoSuchKey` غير مفهومة |
| `api/verify-pay.js` | فصل «تم الدفع» عن «تم التسليم» | فشل التسليم كان يُقرأ كفشل دفع |
| `api/verify-pay.js` | تتبّع `delivered` لكل طلب | الطلب كان يُسجَّل قبل التسليم، فإن فشل التسليم لا يُعاد الإرسال أبداً |
| `thanks.html` | شاشة «تم الدفع بنجاح» مع رقم الطلب وزر واتساب عند تعثّر التجهيز | لا يجوز إخبار عميل دافع أن دفعته فشلت |
| `api/check-files.js` | فحص جاهزية التسليم للمالك | لتكتشف النقص قبل العميل |
| `supabase-setup.sql` | عمود `delivered` + ترقية للجداول القائمة | يفصل الدفع عن التسليم |

**نقطة مهمة:** لو كان عندك طلب مدفوع سابقاً ولم يُسلَّم — بعد رفع الملفات افتح
لوحة الإدارة واستخدم **تسليم الطلب الآن** بإيميل العميل، أو اطلب منه فتح رابط
`/thanks` نفسه مرة أخرى وسيُسلَّم تلقائياً (لأن `delivered` ما زال false).

---

## قائمة ما قبل الإطلاق

- [ ] الملفات الستة عشر مرفوعة — تحقق بزر الفحص في لوحة الإدارة
- [ ] الـ bucket `files` على وضع **Private**
- [ ] `supabase-setup.sql` شُغّل بعد التحديث (عمود `delivered`)
- [ ] `PAYLINK_MODE=live` مع المفاتيح الحقيقية (سيرفر التجربة لديهم معطوب — مذكور في `pay-start.js`)
- [ ] `ADMIN_PASSWORD` كلمة قوية
- [ ] `GMAIL_APP_PASSWORD` مضبوط وجرّبت وصول إيميل فعلاً
- [ ] عملية شراء تجريبية حقيقية من أولها لآخرها بمبلغ منتج رخيص
- [ ] الأسعار في `_delivery.js` تطابق `index.html` و`checkout.html` (التحقق من المبلغ
      يعتمد عليها — أي اختلاف يرفض الدفعة)
